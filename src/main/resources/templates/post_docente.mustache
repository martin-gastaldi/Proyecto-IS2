<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Listado de Docentes</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-4xl mx-auto bg-white p-6 rounded shadow">
    <h1 class="text-pink-2xl font-bold mb-4 text center">Docentes registrados</h1>

    {{#docentes}}
      <div class="border-b py-3">
        <div class="flex justify-between items-center">
          <div>
            <p class="font-semibold">DNI: {{dni}}</p>
            <p>{{realName}} {{surname}}</p>
            <p class="text-sm text-gray-600">Departamento: {{departament}} - Correo: {{correo}} - Materia: {{nombreMateria}} - Id carrera: {{id_carrera}}</p>
          </div>
        </div>
      </div>
    {{/docentes}}

    {{^docentes}}
      <p class="text-gray-600">No hay docentes cargados.</p>
    {{/docentes}}

    <div class="mt-4">
      <a href="/get_docente" class="bg-pink-600 text-white px-4 py-2 rounded hover:bg-pink-700">Agregar nuevo docente</a>
      <a href="/dashboard" class="ml-2 text-purple-600 hover:underline">Volver al Panel de Control</a>
    </div>
  </div>
</body>
</html>
